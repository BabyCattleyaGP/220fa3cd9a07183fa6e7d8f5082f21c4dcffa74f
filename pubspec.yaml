name: baby_220fa3cd9a07183fa6e7d8f5082f21c4dcffa74f
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  flutter_rating_bar: ^3.0.1+1
  http: ^0.12.1
  intl: ^0.16.1
  query_params: '^0.6.1'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_native_splash: ^0.1.9
  flutter_launcher_icons: "^0.7.3"
  flutter_launcher_name: "^0.0.1"

flutter_launcher_name:
  name: "Project"

flutter_icons:
  android: "launcher_icon" 
  ios: true
  image_path: "assets/images/icon.png"

flutter_native_splash:
  color: "#fc443c"

flutter:
  uses-material-design: true
  assets:
   - assets/images/
   - assets/fonts/
  fonts:
  - family: Nunito
    fonts:
      - asset: assets/fonts/Nunito-Regular.ttf
        weight: 400
